const gameWidth = 500,
    gameHeight = 500,

    mapDimensions = 8,

    terrainEl = document.getElementsByClassName('terrain')[0],
    unitsEl = document.getElementsByClassName('units')[0],

    game = new Game()